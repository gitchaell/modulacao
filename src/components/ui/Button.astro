---
interface Props {
  href?: string;
  variant?: 'primary' | 'outline' | 'ghost';
  class?: string;
  type?: 'button' | 'submit' | 'reset';
  id?: string;
}

const {
  href,
  variant = 'primary',
  class: className = '',
  type = 'button',
  id
} = Astro.props;

const baseStyles = "font-bold text-[11px] tracking-[0.2em] uppercase px-8 py-4 text-center transition-colors flex items-center justify-center gap-2.5 rounded-sm focus-visible";

const variants = {
  primary: "bg-brand-gold text-brand-black hover:bg-brand-white",
  outline: "border border-brand-gold text-brand-gold hover:bg-brand-gold hover:text-brand-black",
  ghost: "text-brand-gold hover:text-brand-white"
};

const combinedClasses = `${baseStyles} ${variants[variant]} ${className}`;
---

{href ? (
  <a href={href} class={combinedClasses} id={id}>
    <slot />
  </a>
) : (
  <button type={type} class={combinedClasses} id={id}>
    <slot />
  </button>
)}
