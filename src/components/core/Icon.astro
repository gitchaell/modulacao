---
import { Icon as AstroIcon } from 'astro-icon/components';

interface Props {
  name: string;
  class?: string;
  size?: number | string;
}

const { name, class: className = '', size = 24 } = Astro.props;
---
<AstroIcon name={name} class={className} width={size} height={size} />
