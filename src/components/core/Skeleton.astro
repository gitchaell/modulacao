---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---
<div class={`animate-pulse bg-gray-200 dark:bg-gray-800 rounded-sm ${className}`} aria-hidden="true"></div>